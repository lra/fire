[package]
name = "fire"
version = "0.3.0"
edition = "2024"

[dependencies]
minifb = "0.28"

# The per-output-pixel upscale/bloom path needs optimizations even for
# plain `cargo run` to hold 60 fps.
[profile.dev]
opt-level = 2
